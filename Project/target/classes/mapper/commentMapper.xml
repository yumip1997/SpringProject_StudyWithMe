<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="commentDAO">
	<!-- 특정 게시글의 댓글 목록 -->
	<select id="getCommentList" parameterType="int"
		resultType="commentVO">
		select
		comment_num, parent_num, num, depth, comment_content, writedate, userid
		from comment
		where num = #{num}
		order by comment_num asc
	</select>
	
	<!-- 댓글 삽입 -->
	<select id="insertComment" parameterType="commentVO">
	insert into comment(parent_num, depth, comment_content, userid)
	values (#{parentNum}, #{depth}, #{commentContent}, #{userId})
	</select>
	
</mapper>